package lookiero.user;

public class UserServiceImpl implements UserService {
    @Override
    public void followUser(String userName, String followUserName) {
//        User user = UserRepository.getInstance().getUser(userName);
//        User followUser = UserRepository.getInstance().getUser(followUserName);
//        user.follow(followUser);
    }
}
